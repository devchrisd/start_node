This is a site build on Nodejs.

Setup

- Download nodejs from https://nodejs.org/en/download/,  and install
- Install package/module. Run in command line:
  npm install
- Start the node server:
 node index.js
